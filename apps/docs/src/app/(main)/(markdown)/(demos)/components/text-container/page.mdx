---
title: Text Container
description: The TextContainer is a simple wrapper component for rendering paragraphs of text at an optimal max-width for readability.
docType: Demo
docGroup: Components
group: Presentation
keywords: [layout, typography, legibility, line-length]
components: [TextContainer]
---

# Text Container

The `TextContainer` is a simple wrapper component for rendering paragraphs of
text at an optimal max-width for readability. The `max-width` and `padding` will
default to `65ch` and `1rem` respectively but can be configured by changing the
[$SASSDOC](text-line-length) and [$SASSDOC](text-container-padding) Sass
variables or using the [$SASSDOC](<typography-set-var(line-length, NEW_VALUE)>)
and [$SASSDOC](<typography-set-var(text-container-padding, NEW_VALUE)>) mixin.

## Simple Example

Wrap the text content in the `TextContainer` component.

```demo source="./SimpleExample.tsx"

```
