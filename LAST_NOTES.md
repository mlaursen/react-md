# Status

- too annoyed at the Menu API so switched to form validation

  - added useTextField `onInvalid` nicities
  - started working on radio required behavior. mostly need to add typedefs

- Started working on MenuBar + hover mode stuffs
- mostly in `useDropdownMenu`
- still need to figure out `DropdownMenu` component
- still need to figure out `DropdownMenuToggle` (combo of `MenuButton` + `MenuItem`)
