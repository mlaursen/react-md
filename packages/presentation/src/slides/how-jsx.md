# What's JSX

- **J**ava**S**cript **X**ML
- Gotchas?
  - `class` -> `className`
  - `style` -> `{ color: "#000", opacity: 0.12 }`
  - camelCase everything else (except `aria-`)
    - `tabIndex`
    - `onClick`
    - `aria-activedescendant`
