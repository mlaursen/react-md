# @react-md/internal-types
This is a private package for reusing type definitions between modules. I could probably just do relative pathing if needed though.

## Usage
```ts
const { IBaseProps } from "@react-md/internal-types";
```