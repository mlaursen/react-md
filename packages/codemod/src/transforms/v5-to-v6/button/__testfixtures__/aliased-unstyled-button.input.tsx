import { UnstyledButton as SimpleButton } from "react-md";
import UnstyledButton from "./UnstyledButton";

export default function Example() {
  return (
    <>
      <UnstyledButton>Custom</UnstyledButton>
      <SimpleButton>React MD</SimpleButton>
    </>
  );
}
