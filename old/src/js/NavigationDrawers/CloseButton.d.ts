import * as React from 'react';
import { Props } from '../index';

export interface CloseButtonProps extends Props {
}

declare const CloseButton: React.ComponentClass<CloseButtonProps>;
export default CloseButton;
